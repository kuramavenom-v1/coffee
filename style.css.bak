* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Cairo', sans-serif;
}

body {
  background: #0e0e10;
  color: #fff;
  line-height: 1.6;
}

/* NAV */
nav {
  display: flex;
  justify-content: center;
  gap: 18px;
  padding: 15px;
  background: #111;
  position: sticky;
  top: 0;
  z-index: 1000;
}

nav a {
  color: #fff;
  text-decoration: none;
  padding: 8px 14px;
  border-radius: 20px;
  transition: 0.3s;
}

nav a:hover {
  background: #e6c07a;
  color: #111;
}

.hero {
  height: 85vh;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;

  background: url('images/hero.jpg') center/cover no-repeat;
  position: relative;
}

/* طبقة شفافة فوق الصورة */
.hero::before {
  content: "";
  position: absolute;
  inset: 0;
  background: rgba(0,0,0,0.55);
  z-index: 1;
}

/* النص */
.hero div {
  position: relative;
  z-index: 2;
  max-width: 600px;
}

.hero h1 {
  font-size: 38px;
  color: #e6c07a;
  margin-bottom: 10px;
}

.hero p {
  color: #ddd;
  font-size: 18px;
} 

/* SECTIONS */
section {
  padding: 60px 20px;
  text-align: center;
}

h2 {
  color: #e6c07a;
  margin-bottom: 20px;
}

/* GRID (لو استخدمته لاحقًا للمنيو) */
.grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 15px;
  max-width: 900px;
  margin: auto;
}

.card {
  background: #1b1b1b;
  padding: 20px;
  border-radius: 12px;
  transition: 0.3s;
}

.card:hover {
  transform: translateY(-6px);
  background: #222;
}

/* FOOTER */
footer {
  text-align: center;
  padding: 20px;
  background: #111;
  color: #777;
  margin-top: 40px;
}

/* MOBILE */
@media (max-width: 600px) {
  .hero h1 {
    font-size: 30px;
  }

  nav {
    flex-wrap: wrap;
    gap: 10px;
  }
  
html {
  font-size: 16px;
}

body {
  overflow-x: hidden;
}

/* يخلي كل شيء مناسب للجوال */
@media (max-width: 768px) {

  nav {
    flex-wrap: wrap;
    gap: 10px;
    font-size: 14px;
  }

  .hero h1 {
    font-size: 28px;
  }

  .hero p {
    font-size: 14px;
  }

  section {
    padding: 40px 15px;
  }

  h2 {
    font-size: 22px;
  }

  .card {
    padding: 16px;
  }
}